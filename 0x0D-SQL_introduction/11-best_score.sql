-- script that lists all records with score >= 10 in the table of the database
-- script that lists all records with score >= 10 in the table
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
